Android (preferred) live IMU capture, TFLite inference, and map UI belong here
in Phase 7. The Python navigation pipeline must stay importable without Android
so Phase 8 can wrap it as an edge engine.

Two-wheeler data collection is deferred.
